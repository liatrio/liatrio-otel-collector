default:
all_set:
  metrics:
    gh.repo.branch.commits.count:
      enabled: true
    gh.repo.branch.total_age:
      enabled: true
    gh.repo.branchMeanAge:
      enabled: true
    gh.repo.branches.count:
      enabled: true
    gh.repo.count:
      enabled: true
    gh.repo.prMeanLife:
      enabled: true
    gh.repo.prStdDev:
      enabled: true
  resource_attributes:
none_set:
  metrics:
    gh.repo.branch.commits.count:
      enabled: false
    gh.repo.branch.total_age:
      enabled: false
    gh.repo.branchMeanAge:
      enabled: false
    gh.repo.branches.count:
      enabled: false
    gh.repo.count:
      enabled: false
    gh.repo.prMeanLife:
      enabled: false
    gh.repo.prStdDev:
      enabled: false
  resource_attributes:
