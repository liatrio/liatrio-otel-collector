---
exporters:
    logging:

receivers:
    httpjson:
        collection_interval: 10s
        initial_delay: 1s
        sample: data

service:
    pipelines:
        metrics:
            receivers: [httpjson]
            processors: []
            exporters: [logging]
