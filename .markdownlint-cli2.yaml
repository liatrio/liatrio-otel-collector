---
# Exclude documentation.md files from linting because they are auto-generated
# by mdatagen and cannot be manually fixed without being overwritten.
ignores:
  - '**/documentation.md'
config:
  # Disabled — markdown files routinely contain URLs, badges, and tables that
  # exceed 80 characters and cannot be reasonably wrapped.
  MD013: false
